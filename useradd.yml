---
 - name: user creation
   hosts: lab1
   tasks:
    - name: creating user
      user: name="{{item}}" state=present
      with_items:
            - vara1
            - vara2
            - vara3
            - vara4
            - vara5

